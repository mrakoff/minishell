////////////////////////////////////////////////////////////////////////////////////////
SIGNALS - SUBSHELL - ENV VARIABLE UPDATE?
-when you run shell inside shell, it should keep track of the level its in so the signals allow to kill subshells
but not the main shell.
var_name: SHLVL
value: 1

env->var_name: SHLVL
env->value++;

-do we use export?



////////////////////////////////////////////////////////////////////////////////////////
HEREDOC PREP after the pipeline is built, then check for heredoc node and handle
////////////////////////////////////////////////////////////////////////////////////////
PARSING:

	WORD TYPE
	-first token is a command
	-if WORD token then set command, check against builtins. 
	-else add to argv

	REDIRECTION TYPE
	-next token has to be target

	PIPE
	-finish command
	-start new one

	go until node.next->null

////////////////////////////////////////////////////////////////////////////////////////
ERROR handling
	-syntaxx ERROR (unmatched operator, nothing after a |)
	-allocation error
	-parsing stuff??

	on syntax error:
		-print msg
		-set $? to 2; convention for syntax
		-return to prompt
	
	If malloc fails  print: fatal error and exit(1).

////////////////////////////////////////////////////////////////////////////////////////



////////////////////////////////////////////////////////////////////////////////////////
ADD SHELL STRUCT IN MAIN

 -t_env *env
 -int last_exit_code

////////////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////////////
